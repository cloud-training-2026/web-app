name: My first pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted,linux, x64]  #or use your custom label
    steps:
      - name: verify I am on the private runner
        run: |
          hostname
          hostnamectl
          echo "This is running on My server, not on github infrastructure"
   
